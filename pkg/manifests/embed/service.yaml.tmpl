apiVersion: v1
kind: Service
metadata:
  name: {{ .AgentName }}-svc
  namespace: agents
spec:
  selector:
    app: {{ .AgentName }}
  ports:
    - port: 18790
      targetPort: 18790
      protocol: TCP
